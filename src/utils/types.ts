export type AuthValues = {
  email: string;
  password: string;
};

export type RegistrationValues = {
  name: string;
  email: string;
  password: string;
};

export type AdValues = {
  group: string;
  subGroup: string;
  title: string;
  description: string;
  shortDescription: string;
  image: string;
  price: string;
};
